{
  "id": "quiz",
  "title": "Event Loop Check",
  "description": "Order sync, microtasks, and tasks with the event loop model.",
  "lessonId": "03.1.3-event-loop",
  "graphIndex": "03.1.3",
  "questions": [
    {
      "id": "q1",
      "prompt": "What prints, and in what order?\n\n```js\nconsole.log(\"start\");\nPromise.resolve().then(() => console.log(\"micro\"));\nsetTimeout(() => console.log(\"timer\"), 0);\nconsole.log(\"end\");\n```",
      "options": [
        { "id": "a", "text": "`start`, `micro`, `end`, `timer`" },
        { "id": "b", "text": "`start`, `end`, `timer`, `micro`" },
        { "id": "c", "text": "`start`, `end`, `micro`, `timer`" },
        { "id": "d", "text": "`start`, `timer`, `end`, `micro`" }
      ],
      "correctOptionId": "c",
      "explanation": "Sync first (`start`, `end`), then drain microtasks (`micro`), then run the timer task (`timer`)."
    },
    {
      "id": "q2",
      "prompt": "In the event-loop mental model, what happens immediately after synchronous code empties the call stack?",
      "options": [
        { "id": "a", "text": "Run one timer task, then drain microtasks" },
        { "id": "b", "text": "Drain the microtask queue completely" },
        { "id": "c", "text": "Block until the next network packet arrives" },
        { "id": "d", "text": "Clear all queued timers" }
      ],
      "correctOptionId": "b",
      "explanation": "After sync work, the engine drains microtasks completely before taking the next task from the task queue."
    },
    {
      "id": "q3",
      "prompt": "How should you classify `Promise.resolve().then(() => console.log(\"B\"))`?",
      "options": [
        { "id": "a", "text": "Sync" },
        { "id": "b", "text": "Microtask" },
        { "id": "c", "text": "Task (macrotask)" },
        { "id": "d", "text": "Parallel thread work" }
      ],
      "correctOptionId": "b",
      "explanation": "Promise reactions (`.then` callbacks) are microtasks, not timer tasks."
    },
    {
      "id": "q4",
      "prompt": "Expected order for:\n\n```js\nconsole.log(\"A\");\nPromise.resolve().then(() => console.log(\"B\"));\nsetTimeout(() => console.log(\"C\"), 0);\nconsole.log(\"D\");\n```",
      "options": [
        { "id": "a", "text": "`A`, `B`, `D`, `C`" },
        { "id": "b", "text": "`A`, `D`, `B`, `C`" },
        { "id": "c", "text": "`A`, `D`, `C`, `B`" },
        { "id": "d", "text": "`A`, `B`, `C`, `D`" }
      ],
      "correctOptionId": "b",
      "explanation": "`A` and `D` are sync. `B` is a microtask and runs before the `setTimeout` task `C`."
    }
  ]
}
