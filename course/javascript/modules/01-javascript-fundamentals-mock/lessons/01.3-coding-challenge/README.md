# Coding Challenge

Implement **Clamp Utility** — read a value and a min/max range from stdin, then print the clamped result.

Open the starter file, implement `clamp(value, min, max)`, and use **Run answer** to verify all test cases.
