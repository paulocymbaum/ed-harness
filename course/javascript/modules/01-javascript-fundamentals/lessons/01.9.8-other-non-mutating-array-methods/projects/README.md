# Projects — Log Level Slicer

Practice exercises for this lesson. Run each project with:

```bash
node starter/index.js < starter/sample.input
```

Validate in the UI with **Delivery → Run answer** (`starter/tests.json`).

## What you should practice

- Concepts from the lesson Predict first / What to observe sections
- Exact stdout matching the acceptance examples

## Folder conventions

- Projects live under `projects/` as `NNN-kebab-name/`
- Each project has `README.md`, `starter/index.js`, `starter/tests.json`, `starter/sample.input`, optional `solution/`

## Project catalog

### 032-log-level-slicer/

Log Level Slicer — slice + includes + join.

## PBL contract checklist

Each project README must include (English headers): Problem context, Goal, Lesson concepts practiced, Functional requirements, Non-functional requirements, Constraints, Acceptance criteria, Example data, Suggested plan, Deliverables, Extensions.
