{
  "version": 2,
  "courseId": "javascript",
  "projectId": "013-validation-doc-block",
  "updatedAt": "2026-07-01T00:53:24.566Z",
  "deliveries": [
    {
      "id": "2026-07-01T00:52:48.604Z",
      "content": "/**\n * Commented Validator\n *\n * Entrypoint: node starter/index.js\n */\n\nconst readline = require(\"readline\");\n\nfunction validate(name, score) {\n  const trimmed = name.trim();\n\n  // Rule 1: name must not be empty after trim\n  if (trimmed.length === 0) {\n    return \"ERROR: name is required\";\n  }\n\n  // Rule 2: score must be 0–100\n  // if (!Number.isInteger(score) || score < 0 || score > 100) {\n  //   return \"ERROR: score out of range\";\n  // }\n\n  // Rule 3: name must be at least 2 chars after trim\n  if (trimmed.length < 2) {\n    return \"ERROR: name too short\";\n  }\n\n  return \"OK\";\n}\n\nasync function main() {\n  const lines = [];\n  const rl = readline.createInterface({ input: process.stdin });\n  for await (const line of rl) {\n    lines.push(line);\n    if (lines.length < 2) continue;\n    const name = lines[0];\n    const score = Number(lines[1].trim());\n    process.stdout.write(validate(name, score) + \"\\n\");\n    rl.close();\n    return;\n  }\n  rl.close();\n}\n\nmain();",
      "submittedAt": "2026-07-01T00:52:48.604Z",
      "review": {
        "score": 100,
        "comment": "All three validation rules have adjacent // comments explaining each check, and rule 2 is correctly disabled by commenting out the score range if. Acceptance cases pass: Ana/200 returns OK, empty name returns required error, single-char name returns too short. Solid match to lesson concepts on using comments to document and disable rules.",
        "reviewedAt": "2026-07-01T00:53:24.566Z"
      }
    }
  ]
}
