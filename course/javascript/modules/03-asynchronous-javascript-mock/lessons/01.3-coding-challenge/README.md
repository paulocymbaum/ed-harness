# Coding Challenge

Implement **Promise Order Labeler** — read a line of `sync`/`micro`/`macro` tokens and print them regrouped in the order the event loop would actually run them: all `sync` tokens first, then all `micro` tokens, then all `macro` tokens.

Open the starter file, implement `classifyTokens(tokens)`, and use **Run answer** to verify all test cases.
