# Coding Challenge

Implement **Shallow Merge Guard** — read two JSON objects from stdin and print a shallow merge of the second onto a copy of the first, without mutating the first.

Open the starter file, implement `shallowMergeGuard(a, b)`, and use **Run answer** to verify all test cases.
